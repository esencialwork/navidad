import React from 'react';

/**
 * A top notification bar with a short promotional message.
 */
export default function TopBanner() {
  return (
    <div className="w-full bg-primary text-white text-center py-2 px-4 text-sm">
      Preventa navideña — Cupos limitados — $1,900 MXN
    </div>
  );
}